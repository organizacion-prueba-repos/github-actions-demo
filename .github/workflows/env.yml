name: Variables Demo

on: push

jobs:
  env-test:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar info
        run: |
          echo "Repositorio: ${{ github.repository }}"
          echo "Autor: ${{ github.actor }}"
