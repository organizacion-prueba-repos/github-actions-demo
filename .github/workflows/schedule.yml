name: Scheduled Job

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar mensaje
        run: |
          echo "Ejecutando tarea programada"
