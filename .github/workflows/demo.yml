name: Demo GitHub Actions

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:

jobs:
  checkout-demo:
    runs-on: ubuntu-latest

    steps:
      - name: Sin checkout
        run: ls

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Con checkout
        run: ls
