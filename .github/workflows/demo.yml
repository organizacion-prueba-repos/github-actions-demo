name: Demo Secrets

on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Usar secret
        run: echo "Deploy ejecutado"
        env:
          TOKEN: ${{ secrets.DEMO_SECRET }}
