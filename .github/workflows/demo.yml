name: Demo GitHub Actions

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:

jobs:
  error-demo:
    runs-on: ubuntu-latest

    steps:
      - name: Falla pero sigue
        run: exit 1
        continue-on-error: true

      - name: Sigue ejecutando
        run: echo "El pipeline sigue en pie"
