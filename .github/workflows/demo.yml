name: Demo Jobs Paralelos

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Construyendo proyecto..."

  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ejecutando pruebas..."
