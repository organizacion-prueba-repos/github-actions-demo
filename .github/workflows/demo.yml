name: Demo GitHub Actions

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:

jobs:
  checkout-advanced:
    runs-on: ubuntu-latest

    steps:
      - name: Repo actual
        uses: actions/checkout@v4

      - name: Otro repo
        uses: actions/checkout@v4
        with:
          repository: actions/checkout
          path: RepositorioPrueba

      - run: ls
