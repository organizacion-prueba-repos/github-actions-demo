name: Demo Multiple Needs

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build listo"

  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lint OK"

  test:
    needs: [build, lint]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tests ejecutados"
