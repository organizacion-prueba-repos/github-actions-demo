name: Demo Needs

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tests ejecutados"
