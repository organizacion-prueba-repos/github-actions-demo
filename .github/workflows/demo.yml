name: Demo Needs

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build listo"

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tests ejecutados"
